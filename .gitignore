# Reale Konfigurationsdateien — systemspezifisch, NIE committen.
# Nur die *.conf.example-Vorlagen gehören ins Repo.
*.conf

# Betriebssystem
.DS_Store
Thumbs.db

# Editor
*.swp
*~

# Temporär
*.tmp
*.bak

# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg

# Test-/Typ-/Lint-Caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtuelle Umgebungen
.venv/
venv/

# Ein-Schritt-Paket (make dist): zur Bauzeit installierte Fremd-Bibliotheken
# und Artefakte. pifos ist als eingebettete Kopie Teil des Repos
# (usr/lib/pifos, versioniert) und hier bewusst NICHT ausgeschlossen.
usr/lib/secure_base/_vendor/
dist/
